package firebase

import (
        "context"
        "firebase.google.com/go/auth"
        "google.golang.org/api/option"
        "os"
        "firebase.google.com/go/v4"
        "github.com/pkg/errors"
        )


var(
    firebaseConfigFile=os.Get(key:"../serviceAccountKey.json")
)


func InitAuth()(*auth.Client,error){

    opt:=option.WithCredentialsFile(firebaseConfigFile)
    app, err:=firebase.NewApp(context.Background(), config:nil,opt)
    if err !=nil{
    return nil, errors.Wrap(err, message:"error initializing firebase auth (create firebase app"))
    }
    client, errAuth

    }


}